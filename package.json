{
  "name": "dynamic-workflows-2026",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler types && wrangler dev",
    "deploy": "wrangler types && wrangler deploy",
    "generate-types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "dynamic-workflows": "workspace:*"
  }
}
